#!/usr/bin/env bash

# Start services on target

set -e

# Config vars
export LANG="<%= env_lang %>"
SERVICE_NAME="<%= service_name %>"

<%= if runtime_environment_service do %>
/bin/systemctl start "${SERVICE_NAME}-runtime-environment"
<% end %>
/bin/systemctl start "${SERVICE_NAME}"
